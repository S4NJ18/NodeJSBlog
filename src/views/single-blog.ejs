<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= singleBlogData.title %> | My Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .blog-content img {
      max-width: 100%;
      height: auto;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <a href="/blogs" class="btn btn-secondary mb-3">← Back to All Blogs</a>

    <h1><%= singleBlogData.title %></h1>
    <p class="text-muted">Published on <%= new Date(singleBlogData.createdAt).toDateString() %></p>
    
    <% if (singleBlogData.image_url) { %>
      <img src="<%= blog.image_url %>" alt="<%= singleBlogData.title %>" class="img-fluid mb-4">
    <% } %>

    <hr>

    <div class="blog-content">
      <%- singleBlogData.content %> <!-- Use <%- %> to allow HTML inside content -->
    </div>
  </div>
</body>
</html>
