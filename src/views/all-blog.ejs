<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    .blog-card img {
      height: 200px;
      object-fit: cover;
    }
  </style>

  <%- include('./pertial/head.ejs') %>
</head>
  <!-- Navbar -->

  <body>

 <%- include('./pertial/header', { title: 'All Blog | MyBlog' }) %>
  <!-- Breadcrumb -->
  <div class="container mt-3 mb-5">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item active" aria-current="page">Home</li>
      </ol>
    </nav>

    <!-- Blog Cards Grid -->
    <div class="row row-cols-1 row-cols-md-3 g-4 pt-4">

        <% if(errormsg) { %>
           <p> <%= errormsg %> </p>
        <% } else { %>


        <% getBlogAllListFinal.slice(0,6).forEach((data, index) => { %>
        <div class="col">
            <div class="card blog-card shadow-sm">
                <img src="<%= data.image || 'https://placehold.co/600x400' %>" class="card-img-top" alt="Blog Image">
                    <div class="card-body">
                        <h5 class="card-title"><%= data.title %></h5>
                        <p class="card-text"><<%= data.previewText %></p>
                        <p class="text-muted">By <%= data.author?.username %> | <%= new Date(data.createdAt).toDateString() %></p>
                        <a href="/blogs/<%= data.slug %>" class="btn btn-primary btn-sm">Read More</a>
        </div>
      </div>
    </div>
  <% }) %>
      

      <% } %>
      
      <!-- Card 2 -->

      <!-- Repeat more cards as needed -->
    </div>
  </div>

  <%- include('./pertial/footer.ejs') %>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
